# Windows / CPU-only requirements for easy-xedu
# This file intentionally omits `torch` so that you can install a CPU-only
# PyTorch wheel explicitly (using the PyTorch cpu index). See the instructions
# below for the exact pip command to install torch for your platform.

BaseNN==2.0.0
Flask==3.1.2
flask_cors==6.0.2
flask_socketio==5.6.0
MMEdu==2.0.0
PySimpleGUI==5.0.8.3
setuptools==80.9.0

# Notes:
# 1) Install CPU-only PyTorch separately before installing the rest of these
#    requirements. Examples:
#    Windows (recommended in CI):
#      python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.10.0
#    Linux (CPU):
#      python -m pip install --index-url https://download.pytorch.org/whl/cpu torch==2.10.0
# 2) If MMEdu or other packages still try to pull mmcv-full and fail, you may
#    need to remove/replace mmcv-full or install a compatible prebuilt wheel.
# 3) Use this file in CI or on Windows to avoid attempting to build CUDA wheels
#    on machines without NVIDIA GPUs.
